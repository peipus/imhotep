<%= form_for(@part, :html => {:multipart =>true}) do |f| %>
  <% if @part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@part.errors.count, "error") %> prohibited this part from being saved:</h2>

      <ul>
      <% @part.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Фотография%><br />
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit "Добавить" %>
  </div>
<% end %>
