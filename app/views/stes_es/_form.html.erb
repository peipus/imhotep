<%= form_for(@stes_e) do |f| %>
  <% if @stes_e.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stes_e.errors.count, "error") %> prohibited this stes_e from being saved:</h2>

      <ul>
      <% @stes_e.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 
  <div class="field">
    <%= f.label :Заголовок %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :Текст %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit "Сохранить изменения"%>
  </div>
<% end %>
